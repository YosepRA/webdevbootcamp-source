Compared to [original repository](https://github.com/nax3t/webdevbootcamp), there won't be any `node_modules` folder. Make sure to run the command below before using any of the source codes.
```
npm install
```